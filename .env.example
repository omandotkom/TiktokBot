# ===============================================================
# == Contoh Konfigurasi Environment untuk TikTok Bot Template   ==
# ===============================================================
# Salin file ini ke .env dan isi nilainya sesuai kebutuhan Anda.
# Jangan pernah memasukkan file .env ke dalam sistem kontrol versi (Git).

# --- Konfigurasi Browser Playwright ---
# Browser yang akan digunakan. Pilihan: chromium, firefox, webkit
PLAYWRIGHT_BROWSER=chromium

# Mode headless. "true" untuk berjalan di background, "false" untuk melihat browser.
# Untuk development dan login awal, disarankan menggunakan "false".
HEADLESS=false

# --- Konfigurasi Sesi & Jaringan ---
# Direktori untuk menyimpan file sesi (cookies, local storage)
SESSION_DIR=./sessions

# Locale default browser (contoh: id-ID)
LOCALE=id-ID

# Timezone ID untuk konteks Playwright (contoh: Asia/Jakarta)
TIMEZONE_ID=Asia/Jakarta

# URL Proxy (opsional). Jika tidak digunakan, biarkan kosong.
# Format: http://<user>:<password>@<host>:<port>
# Contoh: PROXY_URL=http://user123:password@proxy.example.com:8080
PROXY_URL=

# --- Konfigurasi API Pihak Ketiga ---
# API Key untuk layanan Captcha Solver (mis. 2Captcha, Anti-Captcha).
# Jika kosong, bot akan menggunakan solver dummy.
CAPTCHA_API_KEY=

# --- Konfigurasi Perilaku Bot ---
# Batas maksimal jumlah aksi (like, follow, comment) yang dilakukan per jam.
# Ini adalah fitur keamanan dasar untuk menghindari deteksi.
MAX_ACTIONS_PER_HOUR=30

# --- URL Contoh untuk Aksi ---
# URL ini digunakan oleh skrip `run_example.py` dan `main.py`.
# Anda bisa menggantinya dengan target yang Anda inginkan.
EXAMPLE_POST_URL_TO_LIKE="https://www.tiktok.com/@summertime_bnb/video/7534696577030491406"
EXAMPLE_USER_URL_TO_FOLLOW="https://www.tiktok.com/@summertime_bnb"
EXAMPLE_POST_URL_TO_COMMENT="https://www.tiktok.com/@summertime_bnb/video/7534696577030491406"